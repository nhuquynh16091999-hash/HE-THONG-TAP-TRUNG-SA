# ═══ Bí mật — KHÔNG BAO GIỜ commit ═══
.env
.env.*
!.env.example
*.env
bigquery_key.json
bigquery_key_*.json
*-service-account*.json
credentials.json
token.json
*.pem
*.key

# ═══ Dữ liệu người dùng nhập trên dashboard (sao kê 3PL, số tiền thật) ═══
data/

# ═══ Bảng tra nhập tay (giá vốn, combo, tỷ giá) — có số tiền thật ═══
config/manual_data/

# ═══ Node ═══
node_modules/
.next/
.test-build/
*.tsbuildinfo

# ═══ Python ═══
__pycache__/
*.pyc
*.pyo
.venv/
.venv-test/

# ═══ Log & tạm ═══
*.log
logs/
.tmp/
*.bak_*
.DS_Store
Thumbs.db

# ═══ IDE / công cụ ═══
.vscode/
.idea/
*.swp
.vercel/
.worktrees

# ═══ File cá nhân, không chia sẻ ═══
*.local.md
!*.local.md.example
*.xlsx
