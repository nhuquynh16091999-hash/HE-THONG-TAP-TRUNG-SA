"""
sync/core — Shared utilities for TALPHA ETL pipeline.

Modules:
  business_rules  — 8 business rules (revenue, FX, marketer, ads join, etc.)
  bq_writer       — BigQuery LOAD JOB wrappers (free-tier safe)
  pos_client      — Poscake POS API client (6 GCC shops)
  meta_client     — Meta Ads API client (8 ad accounts)
"""
